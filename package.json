{
  "name": "bingo-card-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Generates unique 5x5 bingo cards from a custom term list as a print-ready PDF.",
  "license": "MIT",
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "node scripts/dev.js --skip-setup",
    "setup": "node scripts/dev.js --setup-only"
  }
}
